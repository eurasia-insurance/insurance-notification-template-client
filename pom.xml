<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>tech.lapsa.maven</groupId>
		<artifactId>maven-general</artifactId>
		<version>3.0</version>
	</parent>

	<groupId>tech.lapsa.insurance</groupId>
	<artifactId>insurance-notifier-root</artifactId>
	<version>1.1-SNAPSHOT</version>
	<packaging>pom</packaging>

	<name>Insurance Lib : Notifier</name>

	<modules>
		<module>client</module>
		<module>beans</module>
	</modules>

	<properties>
		<!-- BOMS -->
		<java-commons.version>1.1-SNAPSHOT</java-commons.version>
		<javax-javaee-bom.version>7.0</javax-javaee-bom.version>
		<javax-mail.version>2.0</javax-mail.version>
		<!-- tech -->
		<javax-jms-commons.version>1.1-SNAPSHOT</javax-jms-commons.version>
		<lapsa-text.version>1.1-SNAPSHOT</lapsa-text.version>
		<pushapi-dao.version>1.0</pushapi-dao.version>
		<!-- oldies -->
		<insurance-lib.version>2.4-SNAPSHOT</insurance-lib.version>
		<pushapi-lib.version>1.1</pushapi-lib.version>
		<pushapi-sender-impl-default.version>1.1</pushapi-sender-impl-default.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<!-- BOMS -->
			<dependency>
				<groupId>tech.lapsa.java</groupId>
				<artifactId>java-commons-root</artifactId>
				<version>${java-commons.version}</version>
				<scope>import</scope>
				<type>pom</type>
			</dependency>
			<dependency>
				<groupId>tech.lapsa.javax</groupId>
				<artifactId>javax-javaee-bom</artifactId>
				<version>${javax-javaee-bom.version}</version>
				<scope>import</scope>
				<type>pom</type>
			</dependency>
			<dependency>
				<groupId>tech.lapsa.javax</groupId>
				<artifactId>javax-mail-root</artifactId>
				<version>${javax-mail.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>tech.lapsa.pushapi</groupId>
				<artifactId>pushapi-dao-root</artifactId>
				<version>${pushapi-dao.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<!-- tech -->
			<dependency>
				<groupId>tech.lapsa.javax</groupId>
				<artifactId>javax-jms-commons</artifactId>
				<version>${javax-jms-commons.version}</version>
			</dependency>
			<dependency>
				<groupId>tech.lapsa.lapsa</groupId>
				<artifactId>lapsa-text</artifactId>
				<version>${lapsa-text.version}</version>
			</dependency>
			<!-- own -->
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>insurance-notifier-client</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>insurance-notifier</artifactId>
				<version>${project.version}</version>
				<type>ejb</type>
			</dependency>
			<!-- oldies -->
			<dependency>
				<groupId>com.lapsa.insurance-lib</groupId>
				<artifactId>insurance-root</artifactId>
				<version>${insurance-lib.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>com.lapsa.pushapi-lib</groupId>
				<artifactId>pushapi-root</artifactId>
				<version>${pushapi-lib.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>com.lapsa.pushapi-lib</groupId>
				<artifactId>pushapi-sender-impl-default</artifactId>
				<version>${pushapi-sender-impl-default.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<scm>
		<connection>scm:git:https://gitlab.lapsa.tech/lapsa/insurance/notifier.git</connection>
		<developerConnection>scm:git:https://gitlab.lapsa.tech/lapsa/insurance/notifier.git</developerConnection>
		<tag>HEAD</tag>
	</scm>

	<issueManagement>
		<system>gitlab</system>
		<url>https://gitlab.lapsa.tech/lapsa/insurance/notifier/issues</url>
	</issueManagement>

</project>
